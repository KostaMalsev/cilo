const {routeAPI} = import 'routeAPI.js'


export class routeRest{

  constructor(port) {
      super(port);
  }
};

module.exports = {routeRest};

































































