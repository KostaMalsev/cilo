const express = require('express')
const bodyParser = require('body-parser')
//additional imports


class routeGraph {

  constructor(port) {
    super();
    //Additonal setup...    
  }

  get(conn) {
    //some other impl
  }

  getUsers(conn) {
    //some other impl
  }
  
};
